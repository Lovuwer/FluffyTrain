apiVersion: v1
kind: Service
metadata:
  name: titan-api
  labels:
    app.kubernetes.io/name: titan
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: titan-api
